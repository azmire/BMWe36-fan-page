export declare type FetchedData = {
  comment: string;
  description: string;
  email: string;
  username: string;
  password: string;
  _id: string;
  productionYear: string;
  engine: string;
  comments: Comment[];
};

export declare type Comment = {
  comment: string;
};
